# Convenience Script to use after `install_deps.sh` has run at least once
# If you delete or modify the Python virtualenv, you'll need to re-run
#   the `install_deps.sh` script, instead of just using this one.

pip install ..
